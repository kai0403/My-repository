#define _CRT_SECURE_NO_WARNINGS 1
#pragma warning(disable:6031)
#include "Function.h"
int main()
{
	//int* p = (int*)malloc(sizeof(int) * 10);//向堆区请求分配10个整型数据的空间
	//if (p == NULL)
	//{
	//	printf("%s\n", strerror(errno));//打印错误原因
	//}
	//else
	//{
	//	int i = 0;
	//	for (i = 0; i < 10; i++)
	//	{
	//		*p = i;
	//		printf("%-4d", *p);
	//		p++;
	//	}
	//}
	//free(p);
	//p = NULL;
	int arr[10] = { 0 };
	return 0;
}