#define _CRT_SECURE_NO_WARNINGS 1
#pragma warning(disable:6031)
#include"Function.h"
int main()
{
	/*float input = 0;
	cin >> input;
	int output = (int)input;
	if (input >= 0)
	{
		if (input - output < 0.5)
		{
			cout << output << endl;
		}
		else
		{
			cout << output + 1 << endl;
		}
	}
	else
	{
		if (input - output < -0.5)
		{
			cout << output - 1 << endl;
		}
		else
		{
			cout << output << endl;
		}
	}*/
	/*int num1 = 0;
	int num2 = 0;
	cin >> num1 >> num2;
	while (num1 < -1000 || num1 > 1000 || num2 < -1000 || num2 > 1000)
	{
		cout << "Wrong number, input again!" << endl;
		cin >> num1 >> num2;
	}
	cout << num1 + num2 << endl;*/
	/*int num1 = 0;
	int num2 = 0;
	cin >> num1 >> num2;
	int max = num1 > num2 ? num1 : num2;
	cout << max << endl;*/
	/*int num1 = 0;
	int num2 = 0;
	cin >> num1 >> num2;
	while (num1 > 9999 || num1 < 1 || num2 > 9999 || num2 < 1)
	{
		cout << "Wrong number, input again!" << endl;
		cin >> num1 >> num2;
	}*/
	/*cout << Sum(num1, num2) <<' '<< Sub(num1, num2) << ' ' << Mul(num1, num2) << ' ' << Div(num1, num2) << ' ' << Mold(num1, num2) << endl;*/
	return 0;
}